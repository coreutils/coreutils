AUTOMAKE_OPTIONS = 1.1 gnits

TESTS = mv-special-1
EXTRA_DIST = $(TESTS) setup
TESTS_ENVIRONMENT = \
  DF=../../src/df \
  MKDIR=../../src/mkdir \
  MKNOD=../../src/mknod \
  MV=../../src/mv \
  RM=../../src/rm \
  TOUCH=../../src/touch
