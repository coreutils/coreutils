## Process this file with automake to produce Makefile.in -*-Makefile-*-.
AUTOMAKE_OPTIONS = 1.3 gnits

TESTS = empty-file
EXTRA_DIST = $(TESTS)
TESTS_ENVIRONMENT = \
  DF=../../src/df \
  LN=../../src/ln \
  LS=../../src/ls \
  MKDIR=../../src/mkdir \
  MKNOD=../../src/mknod \
  MV=../../src/mv \
  RM=../../src/rm \
  TOUCH=../../src/touch
